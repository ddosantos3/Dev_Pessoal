# Copie este arquivo para .env.local ou .env para configurar o frontend.
# A URL deve apontar para a API FastAPI (ex: uvicorn, docker, etc).
VITE_API_BASE_URL=http://localhost:8000
# VITE_API_CHAT_PATH=/v1/chat
